#this is my first try to resolution this problem

class Solution(object):
    def twoSum(self, nums, target):
        for i in range(len(nums)):
            for j in range(i+1, len(nums)):
                if nums[i] + nums[j] == target:
                    return [i, j]


#and it is works!

#Runtime: 2796 ms, faster than 34.59% of Python online submissions for Two Sum.
#Memory Usage: 14.2 MB, less than 68.06% of Python online submissions for Two Sum.

#In the next few days I will upload more exercises or a better solution to this exercise
